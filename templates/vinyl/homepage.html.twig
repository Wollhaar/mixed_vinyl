{% extends 'base.html.twig' %}

{% block body %}
<h1>{{ title }}</h1>
<div>
    Tracks:
    <ul>
        {% for track in tracks %}
            <li>{{ track.song }} - <span style="background: beige">{{ track.artist|upper }}</span></li>
        {% endfor %}
    </ul>
</div>
{% endblock %}